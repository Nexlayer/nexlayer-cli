application:
  template:
    name: test-app
    deploymentName: test-app
    registryLogin:
      registry: ghcr.io
      username: test-user
      personalAccessToken: test-token
    pods:
      - type: invalid-type
        name: test-service
        tag: latest
