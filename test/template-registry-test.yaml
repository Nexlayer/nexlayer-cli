application:
  template:
    name: registry-test
    deploymentName: registry-test
    pods:
      - type: nginx
        name: web
        tag: 1.23
        exposeHttp: true
      - type: mongodb
        name: db
        tag: 4.4
        vars:
          - key: MONGO_INITDB_DATABASE
            value: testdb
